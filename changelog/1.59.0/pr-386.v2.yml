type: improvement
improvement:
  description: Make it so that the package identification logic of `okgo` only considers
    packages that are part of the same module as the module in the project root directory.
  links:
  - https://github.com/palantir/okgo/pull/386
